### Note
1. npm install
2. Tạo file .env.production nếu sử dụng env riêng
3. npm run dev

# Templates-Expressjs
 Templates project expressjs

# Dependency
    Cors,
    Axios,
    dotenv,
    jsonwebtoken,
    nodemon,
    body-parser,
    socket.io,
    node-localstorage,
    mongodb,
    mongoose
